rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Open rules for debugging and fixing permissions issues
    match /{path=**} {
      allow read, write: if true;
    }
  }
}